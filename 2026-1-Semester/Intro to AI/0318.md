- Search -> 출발점, 끝점
  - Agent: 환경을 인식, 그 환경에 행동을 취하는 주체.
  - State: agent와 그것의 환경 구성 상태.
  - **Initial State**: 초기 상태. (1st)
  - **Action**: 가능한 행동. Action(s) (Action -> s(행동))
  - **Transition Model**: 특정 상태에서 행동을 취했을 때 결과로 나타나는 상태에 대한 설명. RESULT(s, a) (s => a => RESULT)
  - State Space <- 자판기, all states
    : the set of **all states** reachable from the **initial state** by any sequence of actions.
  - **Goal Test**: way to determine whether a given state is a goal state.
  - **Path Test**
   
  - Optimal Solution -> all solution
  - node: a data sturcture that keeps track of
    - a state
    - a parent (상위 node 기록)
    - an action
    - a path cost =>(확인) 효율성
  - queue: last-in first-out data type.
  - <img width="393" height="371" alt="image" src="https://github.com/user-attachments/assets/cea7e2f3-1b59-4d74-b9fb-1beaa5fd8f8c" />
  - Informed Search: search strategy that uses problem-specific knowledge to find solutions **more efficiently**
  - Greedy(탐욕, 욕망) best-first search
  - Heuristic(경험적) funtion: 현재 노드에서 목표까지 도달하는데 드는 예상 비용 추정하는 함수.
    Ex) Manhatten distance: 현재 위치와 목적지 사이의 가로, 세로 거리 합을 계산해 목표까지의 거리를 예측한다.
 
  - **A*search** <- optimal <- if(조건)
    : g(n) + h(n)이 가장 낮은 노드를 확장.
    - g(n): 시작-현재 노드까지 도달하는데 걸린 비용.
    - h(n): 현재-목표까지 도달할 것으로 예상되는 **추정** 비용
    - optimal 조건
      - h(n) $<=$ 실제 비용
      - h(n)(현재) $<=$ h(n')(후속) + c(비용)
    
  - Adverafial Search(적대적 탐색)
    - MiniMax
      : 서로 최선의 수를 둔다고 가정해 결과값을 역산하여 현재의 수 결정.
      - Mini: 상대 점수 최소화 목표.
      - Max: 자신 점수 최대화 목표.

      - Game
        - S0: Initial State
        - Player(s) <- return
        - Actions(s) <- return
        - Result(s, a) <- return
        - Terminal(s)
        - Utility(s): 누가 이겼는지

      - Ex) {Min}: 상대가 어디에 둬야 이기는지를 고려. (value => 선택)
        <img width="923" height="576" alt="image" src="https://github.com/user-attachments/assets/4eade468-02a8-4fb5-a1e0-14646e4afebd" />

      - Given a state s:
        - MAX picks action a in Actions(s) that produces highest value of *MIN-VALUE(RESULT(s, a))*
        - MIN picks action a in Actions(s) that produces smallest value of *MAX-VALUE(RESULT(s, a))*
       
      - Minimax // 자신의 차례 -> 승패가 끝났는가...(1st)
        function MAX-VALUE(state):
          if TERMINAL(state):
             return UTILITY(state)
          v = - $\infty$ // 높은 값 비교를 위해 충분히 작은 값 설정
          for *action* in ACTIONS(*state*):
            v = MAX(v, MIN-VALUE(RESULT(*state, action*)))
          return v
 
  - Optimizations
    <img width="916" height="506" alt="image" src="https://github.com/user-attachments/assets/f5680323-9723-4275-9ebf-aac98c2059cb" />
    - 1st: 첫번째 줄(맨 아래)에서 Min 4, 3, 2 선택(두번째 줄)
    - 2nd: Max는 먼저 4를 확인 -> 3을 비교 조차x(4보다 이미 작음)
   
  - Alpha-Beta Pruning[푸루닝](가지치기)
    - 배경: total possible chess games = $10^29000$ => 경우의 수를 줄여보자.

    - Depth-Limited MiniMax
      - evaluation function
        : function that estimates the expected utility of the game from a given state.
    










       
    
